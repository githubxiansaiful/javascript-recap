// Keys, values, entries, delete, seal, freeze
const employee = {
    name: 'Alice',
    age: 30,
    position: 'Developer'
}
// Getting keys, values, and entries of the object
const objectKeys = Object.keys(employee); // This will give an array of keys
const objectValues = Object.values(employee); // This will give an array of values
const objectKeysAndValues = Object.entries(employee); // This will give an array of [key, value] pairs

// console.log(objectKeysAndValues);


